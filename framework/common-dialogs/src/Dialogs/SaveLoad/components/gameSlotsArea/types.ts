interface PageSlotsAreaOptions {
  pageIndex: number;
  textureName: string;
  labelText: string;
  x: number;
  y: number;
  onClick: undefined | (() => void);
}
export type { PageSlotsAreaOptions };
